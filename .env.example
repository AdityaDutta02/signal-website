# Turso (libSQL/SQLite). Set both in prod. Unset in dev = filesystem fallback under .data/
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# cal.com slot link. Volatile — current slot is 30 min; swap to a 15-min slot type later.
# Site copy always says "book a 15-min call" regardless of the underlying slot length.
NEXT_PUBLIC_CAL_LINK=https://cal.com/aditya-studioionique/30min
